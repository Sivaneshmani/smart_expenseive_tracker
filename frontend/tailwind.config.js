/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{html,js,jsx,ts,tsx}'], // Add paths to your files
  theme: {
    extend: {}, // You can customize or add to the default theme here
  },
  plugins: [], // Add Tailwind plugins if needed (like forms, typography, etc.)
}
